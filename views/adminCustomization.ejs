<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="SafePath is a platform for reporting bullying incidents and seeking support.">
    <meta name="author" content="Reymel Rey Mislang">
    <title>Admin Dashboard | Bullying Reporting System</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-21ZRHLSG08"></script>
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="https://cdn.datatables.net/1.11.5/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="/script/script.js" defer></script>
    <style>
        body {
            background-color: #f4f7fa;
            font-family: Arial, sans-serif;
            font-size: 0.9rem;
            height: 100vh;
            margin: 0;
            display: flex;
        }
        .sidebar {
            width: 200px;
            background-color: #343a40;
            color: #ffffff;
            padding: 15px;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }
        .sidebar a {
    color: #ffffff;
    text-decoration: none;
    border-radius: 0.25rem; /* Rounded corners for normal state */
    transition: background-color 0.3s; /* Smooth background transition */
}

.sidebar a:hover {
    background-color: rgba(255, 255, 255, 0.1); /* Highlight on hover */
    border-radius: 0.25rem; /* Keep rounded corners on hover */
}

.sidebar .nav-link.active {
    font-weight: bold;
    background-color: rgba(255, 255, 255, 0.2); /* Active link background */
    border-radius: 0.25rem; /* Keep rounded corners on active */
}

        .content {
            flex-grow: 1;
            padding: 20px;
            overflow-y: auto;
        }
        .card {
            border-radius: 0.5rem;
            margin-bottom: 1rem;
            box-shadow: 0 3px 6px rgba(0, 0, 0, 0.1);
        }
        .chart-container {
            background-color: #f9f9f9;
            border-radius: 0.5rem;
            padding: 15px;
            height: 250px;
        }
        .status-card {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
        }
        .status-card .card {
            flex: 1 1 150px;
            text-align: center;
            padding: 15px;
        }
        .badge {
            font-size: 0.85rem;
        }
        .small {
            font-size: 1.0rem;
        }
        .fs-4 {
            font-size: 1.5rem;
        }
    </style>
</head>
<body>
    <div style="display: flex; width: 100%;">
        <nav class="sidebar">
            <a href="/" class="logo d-flex align-items-center mb-3">
                <img src="/images/logomisnu.png" alt="SafePath Logo" class="logo-img mr-2" style="width: 40px;">
                <span class="logo-text font-weight-bold text-white">SafePath</span>
            </a>
            <ul class="nav flex-column" id="navbar">
                <li class="nav-item"><a class="nav-link" href="/admin/dashboard">Home</a></li>
                <li class="nav-item"><a class="nav-link" href="/admin/dashboard/complaints">Complaints</a></li>
                <li class="nav-item"><a class="nav-link " href="/admin/dashboard/feedback">Feedback</a></li>
                <li class="nav-item"><a class="nav-link active" href="/admin/dashboard/customization">Customization</a></li>
                <li class="nav-item"><a class="nav-link" href="/admin/dashboard/customization/why-we-here">Why We’re Here</a></li>
                </ul>
            <a class="nav-link mt-auto" href="/admin/logout">Logout</a>
        </nav>
        <div class="content">
            <header class="bg-light py-2 shadow-sm mb-3">
                <h1 class="h4">Admin / Customization / FAQ</h1>
            </header>
                <div class="row">
                    <!-- Left Side: Forms for Adding FAQ -->
                    <div class="col-md-6">
                        <div class="card mb-4">
                            <div class="card-header">
                                <h4 class="mb-0">Add FAQ</h4>
                            </div>
                            <div class="card-body">
                                <form id="faqForm">
                                    <div class="form-group">
                                        <label for="question">Question:</label>
                                        <input type="text" id="question" class="form-control" placeholder="Enter your question here..." required>
                                    </div>
                                    <div class="form-group">
                                        <label for="answer">Answer:</label>
                                        <textarea id="answer" class="form-control" rows="4" placeholder="Provide the answer here..." required></textarea>
                                    </div>
                                    <button type="submit" class="btn btn-primary btn-block">Add FAQ</button>
                                </form>
                                <div id="faqResponse" class="mt-3"></div>
                            </div>
                        </div>
                    </div>

                    <!-- Right Side: Display Existing FAQs -->
                    <div class="col-md-6">
                        <div class="card mb-4">
                            <div class="card-header">
                                <h4 class="mb-0">Existing FAQs</h4>
                            </div>
                            <div class="card-body" id="faqList">
                                <!-- FAQ items will be populated here -->
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>


    <script>
        // Load data on page load
        loadFAQs();

        // Function to load FAQs from the server
        async function loadFAQs() {
            try {
                const response = await fetch('/admin/dashboard/customization/faq');
                const faqs = await response.json();
                const faqList = document.getElementById('faqList');
                faqList.innerHTML = ''; // Clear existing content

                faqs.forEach(faq => {
                    const faqItem = document.createElement('div');
                    faqItem.classList.add('faq-item');
                    faqItem.innerHTML = `
                        <h5>${faq.question}</h5>
                        <p>${faq.answer}</p>
                        <button class="btn btn-warning btn-sm" onclick="editFAQ(${faq.id}, '${faq.question}', '${faq.answer}')">Edit</button>
                        <hr>
                    `;
                    faqList.appendChild(faqItem);
                });
            } catch (error) {
                console.error('Error loading FAQs:', error);
            }
        }

        // Submit handler for FAQ Form
        document.getElementById('faqForm').addEventListener('submit', async function(event) {
            event.preventDefault();

            const question = document.getElementById('question').value;
            const answer = document.getElementById('answer').value;

            try {
                const response = await fetch('/admin/dashboard/customization/faq', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ question, answer })
                });

                const data = await response.json();
                document.getElementById('faqResponse').innerText = data.message;
                if (response.ok) {
                    document.getElementById('faqForm').reset();
                    loadFAQs(); // Refresh the FAQ list
                }
            } catch (error) {
                console.error('Error adding FAQ:', error);
            }
        });

        // Function to edit FAQ
        function editFAQ(id, question, answer) {
            document.getElementById('question').value = question;
            document.getElementById('answer').value = answer;
            // Set a data attribute for the form to indicate editing
            document.getElementById('faqForm').setAttribute('data-edit-id', id);
        }

    </script>
</div>
